static u32         thing(int* const one) { return 0; }
static u32         other(int const one, i32 two, i32* thr) { return 0; }
static signed char stuff(unsigned long* const one, i32 const* const two) { return 0; }
